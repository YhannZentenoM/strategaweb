---
const { title, subtitle, align } = Astro.props;
---

<div class={`flex flex-col justify-start gap-y-5 text-zinc-800 ${align}`}>
  <h3 class="uppercase text-sm font-semibold tracking-widest">{subtitle}</h3>
  <h1 class="text-4xl md:text-6xl font-semibold text-balance tracking-tight">
    {title}
  </h1>
</div>
